{
  "name": "boostelixbackend",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "composer install --no-dev --optimize-autoloader",
    "start": "php artisan serve --host=0.0.0.0 --port=8080"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "axios": "^1.8.2",
    "concurrently": "^9.0.1",
    "laravel-vite-plugin": "^1.2.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.2.4"
  }
}
